package com.housekeeping.repository;

import com.housekeeping.entity.RecommendRoutine;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RecommendRoutineRepository extends JpaRepository<RecommendRoutine, Long> {
}
